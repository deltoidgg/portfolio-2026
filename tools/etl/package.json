{
  "name": "etl",
  "version": "0.0.0",
  "private": true,
  "description": "Data pipelines: download public datasets and emit versioned Parquet/JSON artifacts via DuckDB.",
  "type": "module",
  "scripts": {
    "gsa": "node src/gsa.ts build",
    "gsa:fetch": "node src/gsa.ts build --fetch",
    "gsa:inspect": "node src/gsa.ts inspect"
  },
  "dependencies": {
    "@duckdb/node-api": "catalog:",
    "datasets": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  }
}
